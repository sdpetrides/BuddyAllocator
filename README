Systems Project 1.0: A better malloc() and free()
Stephen Petrides & Andrew Macneille
6 October 2016

Steps:

	1. Implement malloc() and free() with static char myblock[]
		Buddy Allocator
	2. Add error detection to methods
	3. Write workloads in memgrind.c


algo(m, n):
	if (n <= m.size):
		if (m.allo):
			jumpToPartner
			// more steps
		else if (n == m.size):
			allocate()
		else:
			loop until (n == m.size):
				partition()
	else:
		jumpToNextNBlock()
		// probobly call algo() again